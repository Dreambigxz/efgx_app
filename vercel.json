{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/app" }
    }
  ],
  
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
